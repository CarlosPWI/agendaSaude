# ============================================================
# PRODUÇÃO — backend (Render / Railway / Fly.io)
# Copie os VALORES REAIS para as variáveis de ambiente da sua
# plataforma (NÃO suba este arquivo; ele é só referência).
# ============================================================

# Supabase (do dashboard do projeto)
SUPABASE_URL=https://yxijhkxjftgzyjjtlpgv.supabase.co
SUPABASE_KEY=<anon public key>
SUPABASE_SERVICE_ROLE_KEY=<service_role key>

# URL do FRONTEND em produção (usada no link de recuperação de senha)
FRONTEND_URL=https://agenda-saude-omega.vercel.app

# CORS em produção: apenas o frontend real (sem localhost)
CORS_ORIGINS=https://agenda-saude-omega.vercel.app

# Redis opcional (rate limit compartilhado). Sem isso, usa memória.
# REDIS_URL=redis://<seu-redis>:6379

# Comando de start (Render usa a porta 10000):
#   uvicorn src.main:app --host 0.0.0.0 --port $PORT
